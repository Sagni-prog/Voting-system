const AnnouncementReducer = (state,action) => {
  
    switch(action.type){
    
      case 'GET': 
         return action.announcement
      default:
    }
  }
  
  export default AnnouncementReducer;